<div class="container mx-auto px-4 py-8">
  <div class="flex flex-col items-center justify-center mb-8">
    <%= image_tag "/icon.png", class: "w-24 h-24 mb-4", alt: "ほめほめログ" %>
    <h1 class="text-4xl font-bold text-center">ほめほめログ</h1>
  </div>
  
  <div class="text-center mb-12">
    <p class="text-xl mb-4">小さなことを褒め合って、自己肯定感を高めよう。</p>
  </div>

  <% unless user_signed_in? %>
    <div class="flex flex-col items-center space-y-4">
      <%= button_to "Googleでログイン", user_google_oauth2_omniauth_authorize_path, data: { turbo: false }, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
      <%= button_to "X (Twitter)でログイン", user_twitter2_omniauth_authorize_path, data: { turbo: false }, class: "bg-black hover:bg-gray-800 text-white font-bold py-2 px-4 rounded" %>
      
      <div class="mt-4">
        <%= button_to "ゲストとして利用する", guests_login_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded" %>
      </div>
    </div>
  <% else %>
    <div class="text-center">
      <p class="mb-4">ようこそ、<%= current_user.name || 'ゲスト' %>さん</p>
      <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-red-500 hover:text-red-700" %>
    </div>
  <% end %>
</div>
