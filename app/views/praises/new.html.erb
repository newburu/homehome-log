<div class="container mx-auto px-4 py-8 max-w-4xl">
  <div class="text-center mb-12">
    <h1 class="text-3xl font-bold text-gray-900 mb-4">今の気分は？</h1>
    <p class="text-gray-600">褒めてほしいテーマを選んでください。<br>AIがあなたにぴったりの言葉を贈ります。</p>
  </div>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <% Ai::GroupingService::THEME_MAPPING.each do |key, label| %>
      <%= button_to praises_path, method: :get, params: { theme: key }, class: "w-full text-left bg-white hover:bg-orange-50 border-2 border-transparent hover:border-orange-200 rounded-2xl p-6 transition-all shadow-sm hover:shadow-md group" do %>
        <div class="flex items-center justify-between">
          <span class="font-bold text-lg text-gray-800 group-hover:text-orange-600 transition-colors"><%= label %></span>
          <span class="text-2xl opacity-50 group-hover:opacity-100 transition-opacity">✨</span>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="mt-12 text-center">
    <%= link_to "ランダムにおまかせ", praises_path, class: "inline-block text-gray-500 hover:text-gray-800 underline underline-offset-4 decoration-gray-300 hover:decoration-gray-500" %>
  </div>
</div>
